#include "main.h"

/**
 * mul - function that multiplies two integers.
 * @a: integer to be multuplied
 * @b: integer to be multiplied
 * Return: mul if successful
 */
int mul(int a, int b)
{
	int mul;

	mul = (a * b);
	return (mul);
}
